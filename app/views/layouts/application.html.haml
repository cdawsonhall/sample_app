<!DOCTYPE html>
%html{:lang => "utf-8"}
  %head
    %title #{title}
    
    = render "layouts/stylesheets"

    = csrf_meta_tag
    = javascript_include_tag "application"

  %body
    .container
      = render "layouts/header"
      %section.round.clearfix
        - flash.each do |key, value|
          .flash{ :class => "#{key}" }
            %p 
              = value
        = yield
      = render "layouts/footer"
      = debug(params) if Rails.env.development?